#         These are default parameters
def mathAddition(num1=2, num2=2): # Requires two parameters for the funtion to work
    answer = num1 + num2
    print(f'Result: {answer}')

mathAddition()
mathAddition(-1, 1) # Provided both parameters
